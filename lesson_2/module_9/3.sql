/* 
 * Write the SQL statement to create a table called temperatures to hold the
 * following data:
 */

CREATE TABLE temperatures (
  date date NOT NULL,
  low integer NOT NULL,
  high integer NOT NULL
);
