/*
 * Write a SQL statement that will create a table named birds that can hold the
 * following values:
 */

CREATE TABLE birds (
  name varchar(255),
  length decimal(3,1),
  wingspan decimal(3,1),
  family varchar(255),
  extinct boolean
);
